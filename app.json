{
  "name": "flask-psi",
  "description": "Flask PSI pull request review app",
  "scripts": {
  },
  "env": {
    "DOWNSTREAM_APP": {
      "required": true
    },
    "SENTRY_DSN": {
      "required": true
    }
  },
  "addons": [
    "heroku-postgresql",
    "logentries"
  ],
  "buildpacks": [
    {
      "url": "urn:buildpack:heroku/python"
    }
  ]
}
